<ul>
  {{#each selection as |option|}}
    {{#box-select-option
        option=option
        isActive=(contains option selected)
        onSelect=(action "toggle")}}
      {{yield option}}
    {{/box-select-option}}
  {{/each}}
</ul>