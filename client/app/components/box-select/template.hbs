<ul>
  {{#each choiceList as |choice|}}
    <li class={{if choice.selected 'active'}} {{action 'toggle' choice}}>
      {{choice.label}}
      {{#if choice.icon}}
        <span class={{choice.icon}}></span>
      {{/if}}
    </li>
  {{/each}}
</ul>
