<div class="section-head" data-test-selector="library-list-section">
  <span class="section-title">{{section.status}}</span>
  <span class="section-stats">{{stats}}</span>
</div>

{{#each section.entries as |entry|}}
  {{users/components/library-list-entry entry=entry}}
{{/each}}
