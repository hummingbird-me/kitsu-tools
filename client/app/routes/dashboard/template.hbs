{{#if currentSession.isAuthenticated}}
  <h1>Welcome, {{currentSession.account.name}}</h1>
{{else}}
  <h1>Dashboard</h1>
  {{link-to (t 'auth.sign-in') 'sign-in'}}
  {{link-to (t 'auth.sign-up') 'sign-up'}}
{{/if}}
