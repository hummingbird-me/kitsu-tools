<div class="panel-header">
  <div class="step-instructions col-sm-7">
    <h3>Last Step, Follow People!</h3>
    <p>Follow users to stay up-to-date on everything they're watching, reading and doing.</p>
  </div>
  <div class="next-step col-sm-5">
    {{#link-to 'user.index' currentUser class='btn btn-primary continue-button finish'}}Finish Account Setup{{/link-to}}
  </div>
</div>

<div class="library-tab-wrapper col-sm-8">
  <ul class="library-tabs inline-list">
    <li>
      <a href="#" class="anime-tab active">Featured on Hummingbird</a>
    </li>
  </ul>
</div>

{{!-- 
  <div class="search-section col-sm-4">
    <div class="search-wrapper">
      <i class="fa fa-search"></i>
      <input class="library-search-widget" type="text" placeholder="Search by username...">
    </div>
  </div>
--}}

<div class="featured-user-wrapper">
  {{#each userList}}
    <div class="follow-panel-wrapper">
      <div class="follow-panel">
        {{#link-to 'user' username}}
          <div class="follow-cover" {{bind-attr style=coverImageStyle}}></div>
        {{/link-to}}
        <div class="user-avatar">
          <a {{bind-attr href=userLink}}>
            {{avatar this 'thumb'}}
          </a>
          {{#if isFollowed}}
            <a class="btn follow-button unfollow" {{action "toggleFollow" this}}> Unfollow</a>
          {{else}}
            <a class="btn follow-button" {{action "toggleFollow" this}}> Follow</a>
          {{/if}}
        </div>
        <div class="user-info">
          <h4>{{#link-to 'user' username}}{{username}}{{/link-to}}</h4>
          <p class="mini-bio">
            {{truncatedBio}}
            {{controller.followerCoverUrl}}
          </p>
        </div>
      </div>
    </div>
  {{/each}}
</div>
