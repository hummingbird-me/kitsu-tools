<div class="update-panel">
  <div class="user-image">
    {{#link-to 'user' model.user}}{{avatar model.user "thumb_small"}}{{/link-to}}
  </div>
  <div class="comment-content">
    <h4 class="story-title">
      {{#link-to 'user' model.user}}{{model.user.username}}{{/link-to}}
      followed
      {{pluralize model.substoryCount "user" "users"}}
    </h4>
    <p class="secondary">
      {{time-ago time=model.createdAt}}
      {{#if model.canDeleteStory}}
        ·
        <a class="remove-post" {{action "deleteStory"}}>remove</a>
      {{/if}}
    </p>
  </div>
  <div class="row">
    <div class="comment-text">
      <div class="clearfix"></div>
      <ul class="follow-bubbles">
        {{#each displaySubstories as |substory|}}
          <li>
            {{#link-to 'user' substory.user}}
              <div class="user-bubble">
                <div class="bubble-avatar">
                  {{avatar substory.user "thumb_small"}}
                </div>
                <h4 class="bubble-name">{{substory.user.username}}</h4>
              </div>
            {{/link-to}}
          </li>
        {{/each}}
        {{#if moreThanTwoSubstories}}
          <li class="view-more">
            {{#if loadingAll}}
              <i class="fa fa-spin fa-spinner"></i>
            {{else}}
              <a {{action "toggleShowAll"}}>
                {{#if showAll}}
                  Show Less
                {{else}}
                  Show More
                {{/if}}
              </a>
            {{/if}}
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</div>
