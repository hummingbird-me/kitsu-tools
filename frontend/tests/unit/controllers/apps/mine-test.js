import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:apps/mine', 'AppsMineController', {
  // Specify the other units that are required for this test.
  needs: []
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
